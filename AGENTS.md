## Project Structure
- The base branch is develop
- The cargo package name of `crates/fiber-lib` is `fnn`

## Test
Run test using `cargo nextest`.

- `cargo nextest run --all-targets --all-features` for a full local verification run.
- Use `make test` when you want the exact flags used in automation.
